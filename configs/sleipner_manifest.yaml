seed: 11
output_root: runs/sleipner_manifest

field:
  enabled: true
  mode: manifest
  path: ""
  manifest_path: examples/sleipner_manifest.template.json

evaluation:
  save_figures: true

report:
  title: "CO2Shift field intake validation"
